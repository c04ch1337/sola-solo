[package]
name = "phoenix_extension_example"
version = "0.1.0"
edition = "2021"

# For cdylib/WASM builds, adjust as needed.
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

