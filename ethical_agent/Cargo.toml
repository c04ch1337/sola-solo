[package]
name = "ethical_agent"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["sync"] }
thiserror = "1"
regex = "1"

[dev-dependencies]
tokio = { version = "1.0", features = ["macros", "rt"] }

